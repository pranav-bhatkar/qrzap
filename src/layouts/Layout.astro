---
import '../styles/global.css'

interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!doctype html>
<html lang="en" class="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content="Generate QR codes for URLs, WiFi, phone numbers, email, SMS, vCards and more." />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title}</title>

    <!-- Open Graph -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content="Free QR code generator with web UI, REST API, and MCP server. Supports URL, WiFi, phone, email, SMS, vCard." />
    <meta property="og:image" content="https://qrzap.fun/og.png" />
    <meta property="og:url" content="https://qrzap.fun" />
    <meta property="og:site_name" content="QRzap" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content="Free QR code generator with web UI, REST API, and MCP server." />
    <meta name="twitter:image" content="https://qrzap.fun/og.png" />
  </head>
  <body class="min-h-screen antialiased flex flex-col">
    <div class="hidden lg:flex max-w-6xl mx-auto relative">
      <div class="fixed top-0 bottom-0 w-4 stripe-pattern border-x border-zinc-800/40 z-40" style="left: calc((100vw - 72rem) / 2 - 1rem);"></div>
      <div class="fixed top-0 bottom-0 w-4 stripe-pattern border-x border-zinc-800/40 z-40" style="right: calc((100vw - 72rem) / 2 - 1rem);"></div>
    </div>

    <div class="flex-grow">
      <slot />
    </div>

    <footer>
      <div class="flex flex-col items-center gap-4 px-5 py-10 max-w-6xl mx-auto">
        <a
          href="https://www.pranavbhatkar.me"
          aria-label="Pranav Bhatkar's Portfolio"
          class="logo font-medium font-inter"
        >
          <p class="copyright">&copy;</p>
          <div class="name">
            <p class="codeBy">Code by</p>
            <p class="pranav">Pranav</p>
            <p class="bhatkar">Bhatkar</p>
          </div>
        </a>
        <div class="flex items-center gap-4 text-[9px] uppercase tracking-[0.2em] text-zinc-600">
          <span>QRzap</span>
          <span>&middot;</span>
          <span>&copy; {new Date().getFullYear()}</span>
          <span>&middot;</span>
          <span>No data stored</span>
        </div>
      </div>
      <div class="max-w-6xl mx-auto">
        <div class="relative w-full h-4 border-y border-y-zinc-800 bg-[repeating-linear-gradient(315deg,rgb(39,39,42)_0,rgb(39,39,42)_1px,transparent_0,transparent_50%)] bg-[length:10px_10px] bg-fixed md:h-6"></div>
      </div>
    </footer>
  </body>
</html>
